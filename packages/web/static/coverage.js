const $=s=>document.querySelector(s),e=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const c=await fetch('/api/v1/coverage').then(r=>r.json());$('#total').innerHTML=`<div class="score">${c.totals.VERIFIED_PERCENT}%</div><div class="meta">${c.totals.VERIFIED} von ${c.totals.TOTAL} Datensätzen verifiziert</div>`;$('#sections').innerHTML=Object.entries(c.sections).map(([k,v])=>`<article class="card"><h3>${e(k)}</h3><div class="score" style="font-size:30px">${v.VERIFIED_PERCENT}%</div><div class="bar"><i style="width:${v.VERIFIED_PERCENT}%"></i></div><p class="meta">${v.VERIFIED} verified · ${v.NEEDS_VERIFICATION} offen · ${v.TOTAL} gesamt</p></article>`).join('');const g=await fetch('/api/v1/coverage/gaps?limit=100').then(r=>r.json());$('#gaps').innerHTML=g.map(x=>`<div class="gap"><code>${e(x.entity_id)}</code><strong>${e(x.canonical_name)}</strong><div><span class="pill">${e(x.verification_status)}</span> ${e(x.entity_type)}</div></div>`).join('')||'<p>Keine offenen Lücken.</p>'}fetch('/health').then(r=>r.json()).then(x=>$('#version').textContent=`v${x.version}`);load();