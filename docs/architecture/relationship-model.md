# Relationship Model

Relationships are first-class records rather than duplicated text fields.

Each relationship contains a stable ID, subject, predicate, object, verification state, validity window, and optional notes. Canonical relationships require evidence. Reference-only graphs use the `REF-` namespace and must never be published as canonical gameplay data.
